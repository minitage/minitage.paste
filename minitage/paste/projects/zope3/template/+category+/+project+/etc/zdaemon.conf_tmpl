<runner>
  program bin/paster serve deploy.ini
  daemon on
  transcript $path/var/log/zdaemon.log
  socket-name $path/var/zdaemonsock
  # Enable this to run the child process as a different user
  # user zope
</runner>
